package ir.afarinesh.realhope.shares.repositories;

import ir.afarinesh.realhope.entities.project.SoftwareRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SoftwareRoleSpringJpaRepository extends JpaRepository<SoftwareRole, Long> {
}
